<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Color x:Key="WinBgColor">#FF0C0C0C</Color>
    <Color x:Key="WinPanelColor">#FF121212</Color>
    <Color x:Key="WinHeaderColor">#FF181818</Color>
    <Color x:Key="PaneBorderColor">#FF8A8A8A</Color>
    <Color x:Key="WinBorderColor">#FF2B2B2B</Color>
    <Color x:Key="WinSubtleBorderColor">#FF4E4E4E</Color>
    <Color x:Key="WinTextColor">#FFE2E2E2</Color>
    <Color x:Key="WinMutedTextColor">#FFB5B5B5</Color>
    <Color x:Key="WinAccentColor">#FF8E2020</Color>
    <Color x:Key="InputHoverBorderColor">#FFB3B3B3</Color>
    <Color x:Key="CommandBarBackgroundColor">#FF0B0B0B</Color>
    <Color x:Key="CommandBarPanelColor">#FF121212</Color>
    <Color x:Key="CommandBarButtonColor">#FF1A1A1A</Color>
    <Color x:Key="StatusConnectedColor">#FF59BA59</Color>
    <Color x:Key="StatusFailedColor">#FFE0A144</Color>
    <Color x:Key="StatusBarNeutralColor">#FF111111</Color>
    <Color x:Key="StatusBarFailedColor">#FF2D1F10</Color>

    <Color x:Key="EventsHeaderColor">#FF2B2B2B</Color>
    <Color x:Key="EventsHeaderButtonColor">#FF6A5140</Color>
    <Color x:Key="PerformanceHeaderColor">#FF1B3921</Color>
    <Color x:Key="PerformanceHeaderAccentColor">#FF2F743C</Color>
    <Color x:Key="EtwHeaderColor">#FF4A3217</Color>
    <Color x:Key="EtwFeedHeaderColor">#FF5C3C1D</Color>
    <Color x:Key="EtwTiHeaderColor">#FF4E3318</Color>
    <Color x:Key="HeuristicsHeaderColor">#FF4A2525</Color>
    <Color x:Key="FilesystemHeaderColor">#FF1E3C35</Color>
    <Color x:Key="ProcessRelationsHeaderColor">#FF474020</Color>
    <Color x:Key="IpcUplinkHeaderColor">#FF1F3E43</Color>
    <Color x:Key="MessageBoxBgColor">#FF0C0C0C</Color>
    <Color x:Key="MessageBoxTextColor">#FFE6E6E6</Color>
    <Color x:Key="MessageBoxMutedTextColor">#FFB0B0B0</Color>
    <Color x:Key="ProcessRowDefaultForegroundColor">#FFD8D8D8</Color>
    <Color x:Key="ProcessRowUnsignedBackgroundColor">#33501F1F</Color>
    <Color x:Key="ProcessRowUnsignedBorderColor">#AAE36B6B</Color>
    <Color x:Key="ProcessRowUnsignedForegroundColor">#FFF4D7D7</Color>
    <Color x:Key="ProcessRowAppContainerBackgroundColor">#333A2A58</Color>
    <Color x:Key="ProcessRowAppContainerBorderColor">#99B07AE8</Color>
    <Color x:Key="ProcessRowAppContainerForegroundColor">#FFF5F5F5</Color>
    <Color x:Key="ProcessRowLowIntegrityBackgroundColor">#2D4C411D</Color>
    <Color x:Key="ProcessRowLowIntegrityBorderColor">#88D1B05C</Color>
    <Color x:Key="ProcessRowElevatedBackgroundColor">#242D3C52</Color>
    <Color x:Key="ProcessRowElevatedBorderColor">#708AA8C7</Color>
    <Color x:Key="ProcessRowSystemBackgroundColor">#22232323</Color>
    <Color x:Key="ProcessRowSystemBorderColor">#555A5A5A</Color>
    <Color x:Key="ProcessRowSystemForegroundColor">#FFB8B8B8</Color>
    <Color x:Key="LaneRowEnabledColor">#2A285B2E</Color>
    <Color x:Key="LaneRowDisabledColor">#2A5B2828</Color>

    <SolidColorBrush x:Key="PaneBorderBrush" Color="{StaticResource PaneBorderColor}"/>
    <SolidColorBrush x:Key="WinBgBrush" Color="{StaticResource WinBgColor}"/>
    <SolidColorBrush x:Key="WinPanelBrush" Color="{StaticResource WinPanelColor}"/>
    <SolidColorBrush x:Key="WinHeaderBrush" Color="{StaticResource WinHeaderColor}"/>
    <SolidColorBrush x:Key="WinBorderBrush" Color="{StaticResource WinBorderColor}"/>
    <SolidColorBrush x:Key="WinSubtleBorderBrush" Color="{StaticResource WinSubtleBorderColor}"/>
    <SolidColorBrush x:Key="WinTextBrush" Color="{StaticResource WinTextColor}"/>
    <SolidColorBrush x:Key="WinMutedTextBrush" Color="{StaticResource WinMutedTextColor}"/>
    <SolidColorBrush x:Key="WinAccentBrush" Color="{StaticResource WinAccentColor}"/>
    <SolidColorBrush x:Key="InputHoverBorderBrush" Color="{StaticResource InputHoverBorderColor}"/>
    <SolidColorBrush x:Key="CommandBarBackgroundBrush" Color="{StaticResource CommandBarBackgroundColor}"/>
    <SolidColorBrush x:Key="CommandBarPanelBrush" Color="{StaticResource CommandBarPanelColor}"/>
    <SolidColorBrush x:Key="CommandBarButtonBrush" Color="{StaticResource CommandBarButtonColor}"/>
    <SolidColorBrush x:Key="StatusConnectedBrush" Color="{StaticResource StatusConnectedColor}"/>
    <SolidColorBrush x:Key="StatusFailedBrush" Color="{StaticResource StatusFailedColor}"/>
    <SolidColorBrush x:Key="StatusBarNeutralBrush" Color="{StaticResource StatusBarNeutralColor}"/>
    <SolidColorBrush x:Key="StatusBarFailedBrush" Color="{StaticResource StatusBarFailedColor}"/>

    <SolidColorBrush x:Key="EventsHeaderBrush" Color="{StaticResource EventsHeaderColor}"/>
    <SolidColorBrush x:Key="EventsHeaderButtonBrush" Color="{StaticResource EventsHeaderButtonColor}"/>
    <SolidColorBrush x:Key="PerformanceHeaderBrush" Color="{StaticResource PerformanceHeaderColor}"/>
    <SolidColorBrush x:Key="PerformanceHeaderAccentBrush" Color="{StaticResource PerformanceHeaderAccentColor}"/>
    <SolidColorBrush x:Key="EtwHeaderBrush" Color="{StaticResource EtwHeaderColor}"/>
    <SolidColorBrush x:Key="EtwFeedHeaderBrush" Color="{StaticResource EtwFeedHeaderColor}"/>
    <SolidColorBrush x:Key="EtwTiHeaderBrush" Color="{StaticResource EtwTiHeaderColor}"/>
    <SolidColorBrush x:Key="HeuristicsHeaderBrush" Color="{StaticResource HeuristicsHeaderColor}"/>
    <SolidColorBrush x:Key="FilesystemHeaderBrush" Color="{StaticResource FilesystemHeaderColor}"/>
    <SolidColorBrush x:Key="ProcessRelationsHeaderBrush" Color="{StaticResource ProcessRelationsHeaderColor}"/>
    <SolidColorBrush x:Key="IpcUplinkHeaderBrush" Color="{StaticResource IpcUplinkHeaderColor}"/>
    <SolidColorBrush x:Key="MessageBoxBgBrush" Color="{StaticResource MessageBoxBgColor}"/>
    <SolidColorBrush x:Key="MessageBoxTextBrush" Color="{StaticResource MessageBoxTextColor}"/>
    <SolidColorBrush x:Key="MessageBoxMutedTextBrush" Color="{StaticResource MessageBoxMutedTextColor}"/>
    <SolidColorBrush x:Key="ProcessRowDefaultForegroundBrush" Color="{StaticResource ProcessRowDefaultForegroundColor}"/>
    <SolidColorBrush x:Key="ProcessRowUnsignedBackgroundBrush" Color="{StaticResource ProcessRowUnsignedBackgroundColor}"/>
    <SolidColorBrush x:Key="ProcessRowUnsignedBorderBrush" Color="{StaticResource ProcessRowUnsignedBorderColor}"/>
    <SolidColorBrush x:Key="ProcessRowUnsignedForegroundBrush" Color="{StaticResource ProcessRowUnsignedForegroundColor}"/>
    <SolidColorBrush x:Key="ProcessRowAppContainerBackgroundBrush" Color="{StaticResource ProcessRowAppContainerBackgroundColor}"/>
    <SolidColorBrush x:Key="ProcessRowAppContainerBorderBrush" Color="{StaticResource ProcessRowAppContainerBorderColor}"/>
    <SolidColorBrush x:Key="ProcessRowAppContainerForegroundBrush" Color="{StaticResource ProcessRowAppContainerForegroundColor}"/>
    <SolidColorBrush x:Key="ProcessRowLowIntegrityBackgroundBrush" Color="{StaticResource ProcessRowLowIntegrityBackgroundColor}"/>
    <SolidColorBrush x:Key="ProcessRowLowIntegrityBorderBrush" Color="{StaticResource ProcessRowLowIntegrityBorderColor}"/>
    <SolidColorBrush x:Key="ProcessRowElevatedBackgroundBrush" Color="{StaticResource ProcessRowElevatedBackgroundColor}"/>
    <SolidColorBrush x:Key="ProcessRowElevatedBorderBrush" Color="{StaticResource ProcessRowElevatedBorderColor}"/>
    <SolidColorBrush x:Key="ProcessRowSystemBackgroundBrush" Color="{StaticResource ProcessRowSystemBackgroundColor}"/>
    <SolidColorBrush x:Key="ProcessRowSystemBorderBrush" Color="{StaticResource ProcessRowSystemBorderColor}"/>
    <SolidColorBrush x:Key="ProcessRowSystemForegroundBrush" Color="{StaticResource ProcessRowSystemForegroundColor}"/>
    <SolidColorBrush x:Key="LaneRowEnabledBrush" Color="{StaticResource LaneRowEnabledColor}"/>
    <SolidColorBrush x:Key="LaneRowDisabledBrush" Color="{StaticResource LaneRowDisabledColor}"/>

    <SolidColorBrush x:Key="ExplorerPreviewBackgroundBrush" Color="#FF111111"/>
    <SolidColorBrush x:Key="ExplorerOverlayBrush" Color="#D0181818"/>
    <SolidColorBrush x:Key="NoDataOverlayBrush" Color="#50101010"/>

    <SolidColorBrush x:Key="{x:Static SystemColors.WindowBrushKey}" Color="#FF121212"/>
    <SolidColorBrush x:Key="{x:Static SystemColors.WindowTextBrushKey}" Color="#FFE2E2E2"/>
    <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="#33484848"/>
    <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="#FFF7FAFC"/>
    <SolidColorBrush x:Key="{x:Static SystemColors.ControlBrushKey}" Color="#FF181818"/>
    <SolidColorBrush x:Key="{x:Static SystemColors.ControlTextBrushKey}" Color="#FFE2E2E2"/>
    <SolidColorBrush x:Key="{x:Static SystemColors.ControlLightBrushKey}" Color="#FF202020"/>
    <SolidColorBrush x:Key="{x:Static SystemColors.ControlLightLightBrushKey}" Color="#FF2A2A2A"/>
    <SolidColorBrush x:Key="{x:Static SystemColors.ControlDarkBrushKey}" Color="#FF0F0F0F"/>
    <SolidColorBrush x:Key="{x:Static SystemColors.ControlDarkDarkBrushKey}" Color="#FF0C0C0C"/>
    <SolidColorBrush x:Key="{x:Static SystemColors.MenuBrushKey}" Color="#FF161616"/>
    <SolidColorBrush x:Key="{x:Static SystemColors.MenuTextBrushKey}" Color="#FFE2E2E2"/>

    <Style TargetType="Window">
        <Setter Property="Background" Value="{StaticResource WinBgBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource WinTextBrush}"/>
        <Setter Property="FontFamily" Value="JetBrains Mono, JetBrainsMono Nerd Font, Cascadia Mono, Segoe UI"/>
        <Setter Property="TextOptions.TextFormattingMode" Value="Display"/>
        <Setter Property="UseLayoutRounding" Value="True"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
    </Style>

    <Style TargetType="Control">
        <Setter Property="FontFamily" Value="JetBrains Mono, JetBrainsMono Nerd Font, Cascadia Mono, Segoe UI"/>
    </Style>

    <Style TargetType="ToolTip">
        <Setter Property="Background" Value="{StaticResource WinPanelBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource WinTextBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource WinBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="6"/>
    </Style>

    <Style TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource WinTextBrush}"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>

    <Style TargetType="Label">
        <Setter Property="Foreground" Value="{StaticResource WinMutedTextBrush}"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Margin" Value="0,0,6,0"/>
    </Style>

    <Style TargetType="Button">
        <Setter Property="Background" Value="{StaticResource WinHeaderBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource WinTextBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource WinBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="10,4"/>
        <Setter Property="Margin" Value="4,0"/>
        <Setter Property="MinWidth" Value="70"/>
        <Setter Property="MinHeight" Value="24"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="0">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          Margin="{TemplateBinding Padding}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
        <Setter Property="Background" Value="#FF242424"/>
        <Setter Property="BorderBrush" Value="#FFB3B3B3"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
        <Setter Property="Background" Value="#FF141414"/>
        <Setter Property="BorderBrush" Value="{StaticResource WinAccentBrush}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ToggleButton">
        <Setter Property="Background" Value="{StaticResource WinHeaderBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource WinTextBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource WinBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="10,4"/>
        <Setter Property="Margin" Value="4,0"/>
        <Setter Property="MinWidth" Value="70"/>
        <Setter Property="MinHeight" Value="24"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="0">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          Margin="{TemplateBinding Padding}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#FF242424"/>
                            <Setter Property="BorderBrush" Value="#FFB3B3B3"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background" Value="#FF141414"/>
                            <Setter Property="BorderBrush" Value="{StaticResource WinAccentBrush}"/>
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter Property="Background" Value="#FF181818"/>
                            <Setter Property="BorderBrush" Value="{StaticResource WinAccentBrush}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsChecked" Value="True">
                <Setter Property="Background" Value="#FF141414"/>
                <Setter Property="BorderBrush" Value="{StaticResource WinAccentBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="PaneIconButtonStyle" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
        <Setter Property="MinWidth" Value="24"/>
        <Setter Property="MinHeight" Value="22"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="Margin" Value="4,3,0,3"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="BorderBrush" Value="#00000000"/>
        <Setter Property="Background" Value="#00000000"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="0">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#33333333"/>
                            <Setter Property="BorderBrush" Value="#88AAB2BC"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background" Value="#66404040"/>
                            <Setter Property="BorderBrush" Value="{StaticResource WinAccentBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="PaneFilterComboItemStyle" TargetType="ComboBoxItem" BasedOn="{StaticResource {x:Type ComboBoxItem}}">
        <Setter Property="Background" Value="#FF141414"/>
        <Setter Property="Foreground" Value="{DynamicResource WinTextBrush}"/>
        <Setter Property="Padding" Value="8,3"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
    </Style>

    <Style x:Key="PaneFilterComboStyle" TargetType="ComboBox" BasedOn="{StaticResource {x:Type ComboBox}}">
        <Setter Property="Background" Value="#FF161616"/>
        <Setter Property="Foreground" Value="{DynamicResource WinTextBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource WinSubtleBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="MinHeight" Value="24"/>
        <Setter Property="Padding" Value="6,1"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <Grid SnapsToDevicePixels="True">
                        <Border x:Name="OuterBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="22"/>
                                </Grid.ColumnDefinitions>
                                <ContentPresenter Margin="8,0,4,0"
                                                  VerticalAlignment="Center"
                                                  HorizontalAlignment="Left"
                                                  Content="{TemplateBinding SelectionBoxItem}"
                                                  ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                  ContentStringFormat="{TemplateBinding SelectionBoxItemStringFormat}"/>
                                <ToggleButton x:Name="DropDownToggle"
                                              Grid.Column="1"
                                              Focusable="False"
                                              ClickMode="Press"
                                              BorderBrush="{TemplateBinding BorderBrush}"
                                              BorderThickness="1,0,0,0"
                                              Background="#FF111111"
                                              IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}">
                                    <Path Width="8"
                                          Height="5"
                                          Stretch="Fill"
                                          Fill="{DynamicResource WinTextBrush}"
                                          Data="M 0 0 L 4 5 L 8 0 Z"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center"/>
                                </ToggleButton>
                            </Grid>
                        </Border>

                        <Popup x:Name="PART_Popup"
                               Placement="Bottom"
                               IsOpen="{TemplateBinding IsDropDownOpen}"
                               AllowsTransparency="True"
                               Focusable="False"
                               PopupAnimation="None">
                            <Border MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}"
                                    MaxHeight="{TemplateBinding MaxDropDownHeight}"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="1">
                                <ScrollViewer Margin="0" SnapsToDevicePixels="True">
                                    <ItemsPresenter/>
                                </ScrollViewer>
                            </Border>
                        </Popup>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="OuterBorder" Property="BorderBrush" Value="{DynamicResource InputHoverBorderBrush}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.6"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="TextBox">
        <Setter Property="Background" Value="#FF151515"/>
        <Setter Property="Foreground" Value="{StaticResource WinTextBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource WinBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="6,3"/>
        <Setter Property="Margin" Value="4,0"/>
        <Setter Property="CaretBrush" Value="{StaticResource WinTextBrush}"/>
        <Setter Property="SelectionBrush" Value="#66707070"/>
        <Setter Property="SelectionOpacity" Value="0.9"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
    </Style>

    <Style TargetType="ComboBox">
        <Setter Property="Background" Value="#FF151515"/>
        <Setter Property="Foreground" Value="{StaticResource WinTextBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource WinBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="6,2"/>
        <Setter Property="Margin" Value="4,0"/>
        <Setter Property="MinHeight" Value="24"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Style.Resources>
            <SolidColorBrush x:Key="{x:Static SystemColors.WindowBrushKey}" Color="#FF151515"/>
            <SolidColorBrush x:Key="{x:Static SystemColors.WindowTextBrushKey}" Color="#FFE2E2E2"/>
            <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="#33484848"/>
            <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="#FFF7FAFC"/>
            <SolidColorBrush x:Key="{x:Static SystemColors.ControlBrushKey}" Color="#FF171717"/>
            <SolidColorBrush x:Key="{x:Static SystemColors.ControlTextBrushKey}" Color="#FFE2E2E2"/>
            <SolidColorBrush x:Key="{x:Static SystemColors.ControlLightBrushKey}" Color="#FF222222"/>
            <SolidColorBrush x:Key="{x:Static SystemColors.ControlLightLightBrushKey}" Color="#FF2A2A2A"/>
            <SolidColorBrush x:Key="{x:Static SystemColors.ControlDarkBrushKey}" Color="#FF101010"/>
            <SolidColorBrush x:Key="{x:Static SystemColors.ControlDarkDarkBrushKey}" Color="#FF0C0C0C"/>
            <SolidColorBrush x:Key="{x:Static SystemColors.MenuBrushKey}" Color="#FF161616"/>
            <SolidColorBrush x:Key="{x:Static SystemColors.MenuTextBrushKey}" Color="#FFE2E2E2"/>
        </Style.Resources>
    </Style>

    <Style TargetType="ComboBoxItem">
        <Setter Property="Background" Value="#FF161616"/>
        <Setter Property="Foreground" Value="{StaticResource WinTextBrush}"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Padding" Value="6,2"/>
        <Style.Triggers>
            <Trigger Property="IsHighlighted" Value="True">
                <Setter Property="Background" Value="#FF242424"/>
            </Trigger>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="#33323232"/>
                <Setter Property="Foreground" Value="#FFF7FAFC"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="ListBox">
        <Setter Property="Background" Value="{StaticResource WinPanelBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource WinTextBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource WinBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="ScrollViewer.CanContentScroll" Value="True"/>
        <Setter Property="VirtualizingPanel.IsVirtualizing" Value="True"/>
        <Setter Property="VirtualizingPanel.VirtualizationMode" Value="Recycling"/>
    </Style>

    <Style TargetType="ListBoxItem">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{StaticResource WinTextBrush}"/>
        <Setter Property="Padding" Value="6,4"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListBoxItem">
                    <Border Background="{TemplateBinding Background}">
                        <ContentPresenter/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#2C2F2F2F"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#22262626"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ListBox" x:Key="ExplorerListStyle" BasedOn="{StaticResource {x:Type ListBox}}">
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="8"/>
    </Style>

    <Style TargetType="ListBoxItem" x:Key="ExplorerItemStyle" BasedOn="{StaticResource {x:Type ListBoxItem}}">
        <Setter Property="Padding" Value="0"/>
        <Setter Property="Margin" Value="0,0,0,8"/>
    </Style>

    <Style TargetType="DataGrid" x:Key="AppDataGridStyle">
        <Setter Property="Background" Value="{StaticResource WinPanelBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource WinTextBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource WinBorderBrush}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="GridLinesVisibility" Value="Horizontal"/>
        <Setter Property="HorizontalGridLinesBrush" Value="{StaticResource WinSubtleBorderBrush}"/>
        <Setter Property="VerticalGridLinesBrush" Value="#00000000"/>
        <Setter Property="HeadersVisibility" Value="Column"/>
        <Setter Property="RowBackground" Value="#FF161616"/>
        <Setter Property="AlternatingRowBackground" Value="#FF1A1A1A"/>
        <Setter Property="RowHeaderWidth" Value="0"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="EnableRowVirtualization" Value="True"/>
        <Setter Property="EnableColumnVirtualization" Value="True"/>
        <Setter Property="ScrollViewer.CanContentScroll" Value="True"/>
        <Setter Property="VirtualizingPanel.IsVirtualizing" Value="True"/>
        <Setter Property="VirtualizingPanel.VirtualizationMode" Value="Recycling"/>
    </Style>

    <Style TargetType="DataGrid" BasedOn="{StaticResource AppDataGridStyle}"/>

    <Style TargetType="DataGridColumnHeader" x:Key="AppDataGridColumnHeaderStyle">
        <Setter Property="Background" Value="#FF1B1B1B"/>
        <Setter Property="Foreground" Value="{StaticResource WinTextBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource WinBorderBrush}"/>
        <Setter Property="BorderThickness" Value="0,0,0,1"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Padding" Value="8,5"/>
    </Style>

    <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource AppDataGridColumnHeaderStyle}"/>

    <Style TargetType="DataGridRow" x:Key="AppDataGridRowStyle">
        <Setter Property="Background" Value="#FF161616"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#FF202020"/>
            </Trigger>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="#33323232"/>
                <Setter Property="Foreground" Value="#FFF7FAFC"/>
                <Setter Property="BorderBrush" Value="Transparent"/>
                <Setter Property="BorderThickness" Value="0"/>
            </Trigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsSelected" Value="True"/>
                    <Condition Property="Selector.IsSelectionActive" Value="False"/>
                </MultiTrigger.Conditions>
                <Setter Property="Background" Value="#33323232"/>
                <Setter Property="Foreground" Value="#FFF7FAFC"/>
            </MultiTrigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="DataGridRow" BasedOn="{StaticResource AppDataGridRowStyle}"/>

    <Style TargetType="DataGridCell" x:Key="AppDataGridCellStyle">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderBrush" Value="{StaticResource WinSubtleBorderBrush}"/>
        <Setter Property="BorderThickness" Value="0,0,0,1"/>
        <Setter Property="Padding" Value="6,0"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="DataGridCell">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          Margin="{TemplateBinding Padding}"
                                          SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="#33323232"/>
                <Setter Property="Foreground" Value="#FFF7FAFC"/>
                <Setter Property="BorderBrush" Value="{StaticResource WinSubtleBorderBrush}"/>
            </Trigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsSelected" Value="True"/>
                    <Condition Property="Selector.IsSelectionActive" Value="False"/>
                </MultiTrigger.Conditions>
                <Setter Property="Background" Value="#33323232"/>
                <Setter Property="Foreground" Value="#FFF7FAFC"/>
                <Setter Property="BorderBrush" Value="{StaticResource WinSubtleBorderBrush}"/>
            </MultiTrigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="DataGridCell" BasedOn="{StaticResource AppDataGridCellStyle}"/>

    <Style TargetType="TabControl">
        <Setter Property="Background" Value="{StaticResource WinHeaderBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource WinBorderBrush}"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabControl">
                    <Grid KeyboardNavigation.TabNavigation="Local">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <Border Grid.Row="0"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="0,0,0,1">
                            <TabPanel x:Name="HeaderPanel"
                                      IsItemsHost="True"
                                      Background="{TemplateBinding Background}"
                                      Margin="2,0,2,0"/>
                        </Border>

                        <Border Grid.Row="1"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="0">
                            <ContentPresenter x:Name="PART_SelectedContentHost"
                                              ContentSource="SelectedContent"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="TabItem">
        <Setter Property="Background" Value="#FF141414"/>
        <Setter Property="Foreground" Value="{StaticResource WinMutedTextBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource WinBorderBrush}"/>
        <Setter Property="Padding" Value="10,4"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabItem">
                    <Border x:Name="TabBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="1,1,1,0"
                            Margin="0,0,4,0"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter ContentSource="Header"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="TabBorder" Property="Background" Value="#FF1C1C1C"/>
                            <Setter Property="Foreground" Value="{StaticResource WinTextBrush}"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="TabBorder" Property="Background" Value="#FF121212"/>
                            <Setter TargetName="TabBorder" Property="BorderBrush" Value="#FF3A3A3A"/>
                            <Setter Property="Foreground" Value="{StaticResource WinTextBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="ScrollBarRepeatButtonStyle" TargetType="RepeatButton">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RepeatButton">
                    <Border Background="{TemplateBinding Background}"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="Thumb">
        <Setter Property="Background" Value="#FF6E7781"/>
        <Setter Property="BorderBrush" Value="#FF9AA4AE"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Thumb">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="0"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#FF8B95A0"/>
            </Trigger>
            <Trigger Property="IsDragging" Value="True">
                <Setter Property="Background" Value="#FFA9B3BD"/>
                <Setter Property="BorderBrush" Value="{StaticResource WinAccentBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="ScrollBar">
        <Setter Property="Background" Value="#0A1A1A1A"/>
        <Setter Property="Foreground" Value="{StaticResource WinMutedTextBrush}"/>
        <Setter Property="Width" Value="8"/>
        <Setter Property="Height" Value="8"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ScrollBar">
                    <Grid Background="{TemplateBinding Background}"
                          SnapsToDevicePixels="True">
                        <Track x:Name="PART_Track"
                               Orientation="{TemplateBinding Orientation}">
                            <Track.DecreaseRepeatButton>
                                <RepeatButton Style="{StaticResource ScrollBarRepeatButtonStyle}"/>
                            </Track.DecreaseRepeatButton>
                            <Track.Thumb>
                                <Thumb/>
                            </Track.Thumb>
                            <Track.IncreaseRepeatButton>
                                <RepeatButton Style="{StaticResource ScrollBarRepeatButtonStyle}"/>
                            </Track.IncreaseRepeatButton>
                        </Track>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="Orientation" Value="Horizontal">
                            <Setter TargetName="PART_Track" Property="IsDirectionReversed" Value="False"/>
                        </Trigger>
                        <Trigger Property="Orientation" Value="Vertical">
                            <Setter TargetName="PART_Track" Property="IsDirectionReversed" Value="True"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="Orientation" Value="Horizontal">
                <Setter Property="Height" Value="6"/>
                <Setter Property="Width" Value="Auto"/>
            </Trigger>
            <Trigger Property="Orientation" Value="Vertical">
                <Setter Property="Width" Value="8"/>
                <Setter Property="Height" Value="Auto"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="Menu">
        <Setter Property="Background" Value="{StaticResource CommandBarBackgroundBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource WinTextBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource WinBorderBrush}"/>
        <Setter Property="BorderThickness" Value="0,0,0,1"/>
        <Setter Property="Padding" Value="4,1"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
    </Style>

    <Style TargetType="ToolBarTray">
        <Setter Property="Background" Value="{StaticResource CommandBarBackgroundBrush}"/>
    </Style>

    <Style TargetType="ToolBar">
        <Setter Property="Background" Value="{StaticResource CommandBarBackgroundBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource WinSubtleBorderBrush}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="2,2,2,2"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToolBar">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                        <ToolBarPanel IsItemsHost="True"
                                      Margin="{TemplateBinding Padding}"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ContextMenu">
        <Setter Property="Background" Value="{StaticResource WinPanelBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource WinTextBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource WinBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="2"/>
        <Setter Property="HasDropShadow" Value="False"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ContextMenu">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Padding="{TemplateBinding Padding}"
                            SnapsToDevicePixels="True">
                        <StackPanel IsItemsHost="True"
                                    KeyboardNavigation.DirectionalNavigation="Cycle"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="TreeExpanderToggleButtonStyle" TargetType="ToggleButton">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Width" Value="16"/>
        <Setter Property="Height" Value="16"/>
        <Setter Property="Focusable" Value="False"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <Border Background="{TemplateBinding Background}"
                            Width="{TemplateBinding Width}"
                            Height="{TemplateBinding Height}">
                        <TextBlock x:Name="Glyph"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   FontFamily="Segoe UI Symbol"
                                   FontSize="10"
                                   Foreground="{StaticResource WinMutedTextBrush}"
                                   Text="&#x25B8;"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="Glyph" Property="Text" Value="&#x25BE;"/>
                            <Setter TargetName="Glyph" Property="Foreground" Value="{StaticResource WinTextBrush}"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Glyph" Property="Foreground" Value="{StaticResource WinTextBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="TreeView">
        <Setter Property="Background" Value="{StaticResource WinPanelBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource WinTextBrush}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="4"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="ScrollViewer.CanContentScroll" Value="True"/>
        <Setter Property="VirtualizingStackPanel.IsVirtualizing" Value="True"/>
        <Setter Property="VirtualizingStackPanel.VirtualizationMode" Value="Recycling"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TreeView">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Padding="{TemplateBinding Padding}">
                        <ScrollViewer CanContentScroll="{TemplateBinding ScrollViewer.CanContentScroll}"
                                      Focusable="False"
                                      HorizontalScrollBarVisibility="Auto"
                                      VerticalScrollBarVisibility="Auto">
                            <ItemsPresenter/>
                        </ScrollViewer>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="TreeViewItem">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{StaticResource WinTextBrush}"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="2,1"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TreeViewItem">
                    <Grid SnapsToDevicePixels="True">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="18"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <ToggleButton x:Name="Expander"
                                      Grid.Row="0"
                                      Grid.Column="0"
                                      Margin="0,0,2,0"
                                      Style="{StaticResource TreeExpanderToggleButtonStyle}"
                                      IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource TemplatedParent}}"
                                      ClickMode="Press"/>

                        <Border x:Name="HeaderBorder"
                                Grid.Row="0"
                                Grid.Column="1"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter x:Name="PART_Header"
                                              ContentSource="Header"
                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"/>
                        </Border>

                        <ItemsPresenter x:Name="ItemsHost"
                                        Grid.Row="1"
                                        Grid.Column="1"
                                        Margin="18,0,0,0"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="HasItems" Value="False">
                            <Setter TargetName="Expander" Property="Visibility" Value="Hidden"/>
                        </Trigger>
                        <Trigger Property="IsExpanded" Value="False">
                            <Setter TargetName="ItemsHost" Property="Visibility" Value="Collapsed"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="HeaderBorder" Property="Background" Value="#FF1D1D1D"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="HeaderBorder" Property="Background" Value="#22343434"/>
                            <Setter Property="Foreground" Value="#FFF1F1F1"/>
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsSelected" Value="True"/>
                                <Condition Property="IsSelectionActive" Value="False"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="HeaderBorder" Property="Background" Value="#1A3A3A3A"/>
                            <Setter Property="Foreground" Value="{StaticResource WinTextBrush}"/>
                        </MultiTrigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{StaticResource WinMutedTextBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <SolidColorBrush x:Key="Menu.Static.Background" Color="#FF181818"/>
    <SolidColorBrush x:Key="Menu.Static.Border" Color="#FF2B2B2B"/>
    <SolidColorBrush x:Key="Menu.Static.Foreground" Color="#FFD8D8D8"/>
    <SolidColorBrush x:Key="Menu.Disabled.Foreground" Color="#FF7C7C7C"/>
    <SolidColorBrush x:Key="MenuItem.Selected.Background" Color="#FF262626"/>
    <SolidColorBrush x:Key="MenuItem.Selected.Border" Color="#FF3A3A3A"/>
    <SolidColorBrush x:Key="MenuItem.Highlight.Background" Color="#FF262626"/>
    <SolidColorBrush x:Key="MenuItem.Highlight.Border" Color="#FF3A3A3A"/>
    <SolidColorBrush x:Key="MenuItem.Highlight.Disabled.Background" Color="#FF1E1E1E"/>
    <SolidColorBrush x:Key="MenuItem.Highlight.Disabled.Border" Color="#FF303030"/>
    <SolidColorBrush x:Key="MenuItem.Disabled.Foreground" Color="#FF777777"/>
    <Style TargetType="MenuItem">
        <Setter Property="Foreground" Value="{StaticResource WinTextBrush}"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="8,4"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="MenuItem">
                    <Grid SnapsToDevicePixels="True">
                        <Border x:Name="Chrome"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <DockPanel LastChildFill="True">
                                <TextBlock x:Name="SubmenuArrow"
                                           DockPanel.Dock="Right"
                                           Margin="12,0,0,0"
                                           Text="&#x203A;"
                                           Visibility="Collapsed"
                                           Foreground="{TemplateBinding Foreground}"/>
                                <ContentPresenter ContentSource="Header"
                                                  RecognizesAccessKey="True"
                                                  VerticalAlignment="Center"/>
                            </DockPanel>
                        </Border>

                        <Popup x:Name="SubmenuPopup"
                               AllowsTransparency="True"
                               Focusable="False"
                               IsOpen="{TemplateBinding IsSubmenuOpen}"
                               PopupAnimation="None"
                               Placement="Bottom">
                            <Border x:Name="SubmenuBorder"
                                    Background="{StaticResource WinPanelBrush}"
                                    BorderBrush="{StaticResource WinBorderBrush}"
                                    BorderThickness="1"
                                    Padding="2">
                                <StackPanel IsItemsHost="True"
                                            KeyboardNavigation.DirectionalNavigation="Cycle"/>
                            </Border>
                        </Popup>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="Role" Value="TopLevelHeader">
                            <Setter TargetName="Chrome" Property="BorderThickness" Value="0"/>
                            <Setter TargetName="Chrome" Property="Padding" Value="8,4"/>
                            <Setter TargetName="SubmenuPopup" Property="Placement" Value="Bottom"/>
                        </Trigger>
                        <Trigger Property="Role" Value="SubmenuHeader">
                            <Setter TargetName="SubmenuArrow" Property="Visibility" Value="Visible"/>
                            <Setter TargetName="SubmenuPopup" Property="Placement" Value="Right"/>
                        </Trigger>
                        <Trigger Property="Role" Value="SubmenuItem">
                            <Setter TargetName="Chrome" Property="Padding" Value="8,4"/>
                        </Trigger>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter TargetName="Chrome" Property="Background" Value="#FF262626"/>
                            <Setter TargetName="Chrome" Property="BorderBrush" Value="#FF3A3A3A"/>
                        </Trigger>
                        <Trigger Property="IsSubmenuOpen" Value="True">
                            <Setter TargetName="Chrome" Property="Background" Value="#FF202020"/>
                            <Setter TargetName="Chrome" Property="BorderBrush" Value="#FF343434"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="#FF777777"/>
                            <Setter TargetName="SubmenuArrow" Property="Foreground" Value="#FF777777"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="Role" Value="SubmenuItem">
                <Setter Property="Background" Value="#FF181818"/>
            </Trigger>
            <Trigger Property="Role" Value="SubmenuHeader">
                <Setter Property="Background" Value="#FF181818"/>
            </Trigger>
            <Trigger Property="IsHighlighted" Value="True">
                <Setter Property="Background" Value="#FF262626"/>
                <Setter Property="BorderBrush" Value="#FF3A3A3A"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Foreground" Value="#FF777777"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="Separator">
        <Setter Property="Background" Value="{StaticResource WinSubtleBorderBrush}"/>
        <Setter Property="Margin" Value="4,2"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Separator">
                    <Border Height="1"
                            Margin="{TemplateBinding Margin}"
                            Background="{TemplateBinding Background}"
                            SnapsToDevicePixels="True"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="GridSplitter">
        <Setter Property="Background" Value="{StaticResource WinSubtleBorderBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource WinSubtleBorderBrush}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="ShowsPreview" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="GridSplitter">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#FF383838"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="SecondaryWindowTitleBarBorderStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource WinHeaderBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource WinBorderBrush}"/>
        <Setter Property="BorderThickness" Value="0,0,0,1"/>
        <Setter Property="Padding" Value="12,8"/>
    </Style>

    <Style x:Key="SecondaryWindowTitleTextStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Foreground" Value="{StaticResource WinTextBrush}"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>

    <Style x:Key="SecondaryWindowSubtitleTextStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="11"/>
        <Setter Property="Foreground" Value="{StaticResource WinMutedTextBrush}"/>
        <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Margin" Value="0,2,0,0"/>
    </Style>

    <Style x:Key="SecondaryWindowStateBadgeStyle" TargetType="Border">
        <Setter Property="Padding" Value="8,2"/>
        <Setter Property="Margin" Value="8,0,0,0"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="BorderBrush" Value="{StaticResource WinBorderBrush}"/>
        <Setter Property="Background" Value="{StaticResource WinPanelBrush}"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>

    <Style x:Key="SecondaryWindowTitleGlyphButtonStyle" TargetType="Button">
        <Setter Property="Width" Value="30"/>
        <Setter Property="Height" Value="24"/>
        <Setter Property="MinWidth" Value="30"/>
        <Setter Property="MinHeight" Value="24"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="Margin" Value="4,0,0,0"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{StaticResource WinTextBrush}"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="FontFamily" Value="Segoe MDL2 Assets"/>
        <Setter Property="FontSize" Value="10"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Chrome"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Chrome" Property="Background" Value="#FF2A2A2A"/>
                            <Setter TargetName="Chrome" Property="BorderBrush" Value="#FF3A3A3A"/>
                            <Setter TargetName="Chrome" Property="BorderThickness" Value="1"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Chrome" Property="Background" Value="#FF1D1D1D"/>
                            <Setter TargetName="Chrome" Property="BorderBrush" Value="{StaticResource WinAccentBrush}"/>
                            <Setter TargetName="Chrome" Property="BorderThickness" Value="1"/>
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="Tag" Value="Close"/>
                                <Condition Property="IsMouseOver" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="Chrome" Property="Background" Value="#FFAA3A3A"/>
                            <Setter TargetName="Chrome" Property="BorderBrush" Value="#FFBC5454"/>
                            <Setter Property="Foreground" Value="#FFF7EDED"/>
                            <Setter TargetName="Chrome" Property="BorderThickness" Value="1"/>
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="Tag" Value="Close"/>
                                <Condition Property="IsPressed" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="Chrome" Property="Background" Value="#FF912E2E"/>
                            <Setter TargetName="Chrome" Property="BorderBrush" Value="#FFBC5454"/>
                            <Setter Property="Foreground" Value="#FFF7EDED"/>
                            <Setter TargetName="Chrome" Property="BorderThickness" Value="1"/>
                        </MultiTrigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="SecondaryWindowSurfaceBorderStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource WinPanelBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource WinBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="8"/>
    </Style>

    <Style x:Key="SecondaryWindowSectionHeaderStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="11"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Foreground" Value="{StaticResource WinMutedTextBrush}"/>
        <Setter Property="Margin" Value="0,8,0,4"/>
    </Style>

    <Style x:Key="SecondaryWindowFilterBarStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource WinPanelBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource WinSubtleBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="8"/>
    </Style>

    <Style x:Key="SecondaryDenseDataGridStyle" TargetType="DataGrid" BasedOn="{StaticResource AppDataGridStyle}">
        <Setter Property="RowHeight" Value="24"/>
        <Setter Property="ColumnHeaderHeight" Value="24"/>
        <Setter Property="CanUserResizeRows" Value="False"/>
        <Setter Property="EnableRowVirtualization" Value="True"/>
        <Setter Property="EnableColumnVirtualization" Value="True"/>
        <Setter Property="ScrollViewer.CanContentScroll" Value="True"/>
        <Setter Property="VirtualizingPanel.IsVirtualizing" Value="True"/>
        <Setter Property="VirtualizingPanel.IsVirtualizingWhenGrouping" Value="True"/>
        <Setter Property="VirtualizingPanel.VirtualizationMode" Value="Recycling"/>
    </Style>
</ResourceDictionary>


