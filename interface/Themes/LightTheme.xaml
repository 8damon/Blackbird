<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Color x:Key="WinBgColor">#FFF6F7F8</Color>
    <Color x:Key="WinPanelColor">#FFFFFFFF</Color>
    <Color x:Key="WinHeaderColor">#FFF2F3F5</Color>
    <Color x:Key="WinBorderColor">#FFD4D8DE</Color>
    <Color x:Key="WinSubtleBorderColor">#FFE1E4E8</Color>
    <Color x:Key="WinTextColor">#FF1F2328</Color>
    <Color x:Key="WinMutedTextColor">#FF5B6470</Color>
    <Color x:Key="WinAccentColor">#FF4C8FD2</Color>

    <Color x:Key="EventsHeaderColor">#FFDCEBFA</Color>
    <Color x:Key="EventsHeaderButtonColor">#FFBCD8F4</Color>
    <Color x:Key="PerformanceHeaderColor">#FFDDF2DD</Color>
    <Color x:Key="PerformanceHeaderAccentColor">#FF88C188</Color>
    <Color x:Key="EtwHeaderColor">#FFF3E3C9</Color>
    <Color x:Key="EtwFeedHeaderColor">#FFEACB99</Color>
    <Color x:Key="EtwTiHeaderColor">#FFE5D7FA</Color>
    <Color x:Key="HeuristicsHeaderColor">#FFF8D7D7</Color>
    <Color x:Key="MessageBoxBgColor">#FF080808</Color>
    <Color x:Key="MessageBoxTextColor">#FFE6E6E6</Color>
    <Color x:Key="MessageBoxMutedTextColor">#FFB0B0B0</Color>

    <SolidColorBrush x:Key="WinBgBrush" Color="{StaticResource WinBgColor}"/>
    <SolidColorBrush x:Key="WinPanelBrush" Color="{StaticResource WinPanelColor}"/>
    <SolidColorBrush x:Key="WinHeaderBrush" Color="{StaticResource WinHeaderColor}"/>
    <SolidColorBrush x:Key="WinBorderBrush" Color="{StaticResource WinBorderColor}"/>
    <SolidColorBrush x:Key="WinSubtleBorderBrush" Color="{StaticResource WinSubtleBorderColor}"/>
    <SolidColorBrush x:Key="WinTextBrush" Color="{StaticResource WinTextColor}"/>
    <SolidColorBrush x:Key="WinMutedTextBrush" Color="{StaticResource WinMutedTextColor}"/>
    <SolidColorBrush x:Key="WinAccentBrush" Color="{StaticResource WinAccentColor}"/>

    <SolidColorBrush x:Key="EventsHeaderBrush" Color="{StaticResource EventsHeaderColor}"/>
    <SolidColorBrush x:Key="EventsHeaderButtonBrush" Color="{StaticResource EventsHeaderButtonColor}"/>
    <SolidColorBrush x:Key="PerformanceHeaderBrush" Color="{StaticResource PerformanceHeaderColor}"/>
    <SolidColorBrush x:Key="PerformanceHeaderAccentBrush" Color="{StaticResource PerformanceHeaderAccentColor}"/>
    <SolidColorBrush x:Key="EtwHeaderBrush" Color="{StaticResource EtwHeaderColor}"/>
    <SolidColorBrush x:Key="EtwFeedHeaderBrush" Color="{StaticResource EtwFeedHeaderColor}"/>
    <SolidColorBrush x:Key="EtwTiHeaderBrush" Color="{StaticResource EtwTiHeaderColor}"/>
    <SolidColorBrush x:Key="HeuristicsHeaderBrush" Color="{StaticResource HeuristicsHeaderColor}"/>
    <SolidColorBrush x:Key="MessageBoxBgBrush" Color="{StaticResource MessageBoxBgColor}"/>
    <SolidColorBrush x:Key="MessageBoxTextBrush" Color="{StaticResource MessageBoxTextColor}"/>
    <SolidColorBrush x:Key="MessageBoxMutedTextBrush" Color="{StaticResource MessageBoxMutedTextColor}"/>

    <SolidColorBrush x:Key="ExplorerPreviewBackgroundBrush" Color="#FFF3F4F6"/>
    <SolidColorBrush x:Key="ExplorerOverlayBrush" Color="#CCE9EDF2"/>
    <SolidColorBrush x:Key="NoDataOverlayBrush" Color="#55FFFFFF"/>

    <SolidColorBrush x:Key="{x:Static SystemColors.WindowBrushKey}" Color="#FFFFFFFF"/>
    <SolidColorBrush x:Key="{x:Static SystemColors.WindowTextBrushKey}" Color="#FF1F2328"/>
    <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="#334C8FD2"/>
    <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="#FF0E2340"/>
    <SolidColorBrush x:Key="{x:Static SystemColors.ControlBrushKey}" Color="#FFF2F3F5"/>
    <SolidColorBrush x:Key="{x:Static SystemColors.ControlTextBrushKey}" Color="#FF1F2328"/>
    <SolidColorBrush x:Key="{x:Static SystemColors.MenuBrushKey}" Color="#FFFFFFFF"/>
    <SolidColorBrush x:Key="{x:Static SystemColors.MenuTextBrushKey}" Color="#FF1F2328"/>

    <Style TargetType="Window">
        <Setter Property="Background" Value="{StaticResource WinBgBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource WinTextBrush}"/>
        <Setter Property="FontFamily" Value="Segoe UI"/>
        <Setter Property="TextOptions.TextFormattingMode" Value="Display"/>
        <Setter Property="UseLayoutRounding" Value="True"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
    </Style>

    <Style TargetType="ToolTip">
        <Setter Property="Background" Value="{StaticResource WinPanelBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource WinTextBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource WinBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="6"/>
    </Style>

    <Style TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource WinTextBrush}"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>

    <Style TargetType="Label">
        <Setter Property="Foreground" Value="{StaticResource WinMutedTextBrush}"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Margin" Value="0,0,6,0"/>
    </Style>

    <Style TargetType="Button">
        <Setter Property="Background" Value="{StaticResource WinHeaderBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource WinTextBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource WinBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="10,4"/>
        <Setter Property="Margin" Value="4,0"/>
        <Setter Property="MinWidth" Value="70"/>
        <Setter Property="MinHeight" Value="24"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="2">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          Margin="{TemplateBinding Padding}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#FFE5E9EF"/>
                            <Setter Property="BorderBrush" Value="#FFB8C3CF"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background" Value="#FFECEFF3"/>
                            <Setter Property="BorderBrush" Value="{StaticResource WinAccentBrush}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ToggleButton">
        <Setter Property="Background" Value="{StaticResource WinHeaderBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource WinTextBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource WinBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="10,4"/>
        <Setter Property="Margin" Value="4,0"/>
        <Setter Property="MinWidth" Value="70"/>
        <Setter Property="MinHeight" Value="24"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="2">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          Margin="{TemplateBinding Padding}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#FFE5E9EF"/>
                            <Setter Property="BorderBrush" Value="#FFB8C3CF"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background" Value="#FFECEFF3"/>
                            <Setter Property="BorderBrush" Value="{StaticResource WinAccentBrush}"/>
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter Property="Background" Value="#FFE7ECF2"/>
                            <Setter Property="BorderBrush" Value="{StaticResource WinAccentBrush}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsChecked" Value="True">
                <Setter Property="Background" Value="#FFE7ECF2"/>
                <Setter Property="BorderBrush" Value="{StaticResource WinAccentBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="PaneIconButtonStyle" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
        <Setter Property="MinWidth" Value="24"/>
        <Setter Property="MinHeight" Value="22"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="Margin" Value="4,3,0,3"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="BorderBrush" Value="#00000000"/>
        <Setter Property="Background" Value="#00000000"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="2">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#339AA6B3"/>
                            <Setter Property="BorderBrush" Value="#66828EA0"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background" Value="#558D99A8"/>
                            <Setter Property="BorderBrush" Value="{StaticResource WinAccentBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="TextBox">
        <Setter Property="Background" Value="#FFFFFFFF"/>
        <Setter Property="Foreground" Value="{StaticResource WinTextBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource WinBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="6,3"/>
        <Setter Property="Margin" Value="4,0"/>
        <Setter Property="CaretBrush" Value="{StaticResource WinTextBrush}"/>
        <Setter Property="SelectionBrush" Value="#66828EA0"/>
        <Setter Property="SelectionOpacity" Value="0.9"/>
    </Style>

    <Style TargetType="ComboBox">
        <Setter Property="Background" Value="#FFFFFFFF"/>
        <Setter Property="Foreground" Value="{StaticResource WinTextBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource WinBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="6,2"/>
        <Setter Property="Margin" Value="4,0"/>
        <Setter Property="MinHeight" Value="24"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Style.Resources>
            <SolidColorBrush x:Key="{x:Static SystemColors.WindowBrushKey}" Color="#FFFFFFFF"/>
            <SolidColorBrush x:Key="{x:Static SystemColors.WindowTextBrushKey}" Color="#FF1F2328"/>
            <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="#334C8FD2"/>
            <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="#FF0E2340"/>
            <SolidColorBrush x:Key="{x:Static SystemColors.ControlBrushKey}" Color="#FFF2F3F5"/>
            <SolidColorBrush x:Key="{x:Static SystemColors.ControlTextBrushKey}" Color="#FF1F2328"/>
            <SolidColorBrush x:Key="{x:Static SystemColors.MenuBrushKey}" Color="#FFFFFFFF"/>
            <SolidColorBrush x:Key="{x:Static SystemColors.MenuTextBrushKey}" Color="#FF1F2328"/>
        </Style.Resources>
    </Style>

    <Style TargetType="ComboBoxItem">
        <Setter Property="Background" Value="#FFFFFFFF"/>
        <Setter Property="Foreground" Value="{StaticResource WinTextBrush}"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Padding" Value="6,2"/>
        <Style.Triggers>
            <Trigger Property="IsHighlighted" Value="True">
                <Setter Property="Background" Value="#FFE5E9EF"/>
            </Trigger>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="#334C8FD2"/>
                <Setter Property="Foreground" Value="#FF0E2340"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="ListBox">
        <Setter Property="Background" Value="{StaticResource WinPanelBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource WinTextBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource WinBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
    </Style>

    <Style TargetType="ListBoxItem">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{StaticResource WinTextBrush}"/>
        <Setter Property="Padding" Value="6,4"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListBoxItem">
                    <Border Background="{TemplateBinding Background}">
                        <ContentPresenter/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#44A3AFBB"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#33AAB7C4"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ListBox" x:Key="ExplorerListStyle" BasedOn="{StaticResource {x:Type ListBox}}">
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="8"/>
    </Style>

    <Style TargetType="ListBoxItem" x:Key="ExplorerItemStyle" BasedOn="{StaticResource {x:Type ListBoxItem}}">
        <Setter Property="Padding" Value="0"/>
        <Setter Property="Margin" Value="0,0,0,8"/>
    </Style>

    <Style TargetType="DataGrid" x:Key="AppDataGridStyle">
        <Setter Property="Background" Value="{StaticResource WinPanelBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource WinTextBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource WinBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="GridLinesVisibility" Value="All"/>
        <Setter Property="HorizontalGridLinesBrush" Value="{StaticResource WinSubtleBorderBrush}"/>
        <Setter Property="VerticalGridLinesBrush" Value="{StaticResource WinSubtleBorderBrush}"/>
        <Setter Property="HeadersVisibility" Value="Column"/>
        <Setter Property="RowBackground" Value="#FFFDFDFD"/>
        <Setter Property="AlternatingRowBackground" Value="#FFFDFDFD"/>
        <Setter Property="RowHeaderWidth" Value="0"/>
        <Setter Property="EnableRowVirtualization" Value="True"/>
        <Setter Property="EnableColumnVirtualization" Value="True"/>
    </Style>

    <Style TargetType="DataGrid" BasedOn="{StaticResource AppDataGridStyle}"/>

    <Style TargetType="DataGridColumnHeader" x:Key="AppDataGridColumnHeaderStyle">
        <Setter Property="Background" Value="#FFEFF2F6"/>
        <Setter Property="Foreground" Value="{StaticResource WinTextBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource WinBorderBrush}"/>
        <Setter Property="BorderThickness" Value="0,0,1,1"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Padding" Value="8,5"/>
    </Style>

    <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource AppDataGridColumnHeaderStyle}"/>

    <Style TargetType="DataGridRow" x:Key="AppDataGridRowStyle">
        <Setter Property="Background" Value="#FFFDFDFD"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#FFF0F4F9"/>
            </Trigger>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="#334C8FD2"/>
                <Setter Property="BorderBrush" Value="Transparent"/>
                <Setter Property="BorderThickness" Value="0"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="DataGridRow" BasedOn="{StaticResource AppDataGridRowStyle}"/>

    <Style TargetType="DataGridCell" x:Key="AppDataGridCellStyle">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderBrush" Value="{StaticResource WinSubtleBorderBrush}"/>
        <Setter Property="BorderThickness" Value="0,0,1,1"/>
        <Setter Property="Padding" Value="6,0"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="DataGridCell">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          Margin="{TemplateBinding Padding}"
                                          SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#1A4C8FD2"/>
            </Trigger>
            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                <Setter Property="BorderBrush" Value="{StaticResource WinAccentBrush}"/>
                <Setter Property="Background" Value="#264C8FD2"/>
            </Trigger>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="#334C8FD2"/>
                <Setter Property="Foreground" Value="#FF0E2340"/>
                <Setter Property="BorderBrush" Value="{StaticResource WinSubtleBorderBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="DataGridCell" BasedOn="{StaticResource AppDataGridCellStyle}"/>

    <Style TargetType="TabControl">
        <Setter Property="Background" Value="{StaticResource WinHeaderBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource WinBorderBrush}"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabControl">
                    <Grid KeyboardNavigation.TabNavigation="Local">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <Border Grid.Row="0"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="0,0,0,1">
                            <TabPanel x:Name="HeaderPanel"
                                      IsItemsHost="True"
                                      Background="{TemplateBinding Background}"
                                      Margin="2,0,2,0"/>
                        </Border>

                        <Border Grid.Row="1"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="0">
                            <ContentPresenter x:Name="PART_SelectedContentHost"
                                              ContentSource="SelectedContent"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="TabItem">
        <Setter Property="Background" Value="#FFE9EDF1"/>
        <Setter Property="Foreground" Value="{StaticResource WinMutedTextBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource WinBorderBrush}"/>
        <Setter Property="Padding" Value="10,4"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabItem">
                    <Border x:Name="TabBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="1,1,1,0"
                            Margin="0,0,4,0"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter ContentSource="Header"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="TabBorder" Property="Background" Value="#FFE1E4E8"/>
                            <Setter Property="Foreground" Value="{StaticResource WinTextBrush}"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="TabBorder" Property="Background" Value="#FFFFFFFF"/>
                            <Setter TargetName="TabBorder" Property="BorderBrush" Value="#FFC8D1DB"/>
                            <Setter Property="Foreground" Value="{StaticResource WinTextBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="ScrollBarRepeatButtonStyle" TargetType="RepeatButton">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RepeatButton">
                    <Border Background="{TemplateBinding Background}"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="Thumb">
        <Setter Property="Background" Value="#FFC8D1DB"/>
        <Setter Property="BorderBrush" Value="#FFA9B6C4"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Thumb">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="2"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#FFBAC6D3"/>
            </Trigger>
            <Trigger Property="IsDragging" Value="True">
                <Setter Property="Background" Value="#FF95A3B4"/>
                <Setter Property="BorderBrush" Value="{StaticResource WinAccentBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="ScrollBar">
        <Setter Property="Background" Value="#FFF3F4F6"/>
        <Setter Property="Foreground" Value="{StaticResource WinMutedTextBrush}"/>
        <Setter Property="Width" Value="12"/>
        <Setter Property="Height" Value="12"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ScrollBar">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{StaticResource WinSubtleBorderBrush}"
                            BorderThickness="1">
                        <Track x:Name="PART_Track">
                            <Track.DecreaseRepeatButton>
                                <RepeatButton Style="{StaticResource ScrollBarRepeatButtonStyle}"/>
                            </Track.DecreaseRepeatButton>
                            <Track.Thumb>
                                <Thumb/>
                            </Track.Thumb>
                            <Track.IncreaseRepeatButton>
                                <RepeatButton Style="{StaticResource ScrollBarRepeatButtonStyle}"/>
                            </Track.IncreaseRepeatButton>
                        </Track>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="Orientation" Value="Horizontal">
                            <Setter TargetName="PART_Track" Property="IsDirectionReversed" Value="False"/>
                        </Trigger>
                        <Trigger Property="Orientation" Value="Vertical">
                            <Setter TargetName="PART_Track" Property="IsDirectionReversed" Value="False"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="Orientation" Value="Horizontal">
                <Setter Property="Height" Value="12"/>
                <Setter Property="Width" Value="Auto"/>
            </Trigger>
            <Trigger Property="Orientation" Value="Vertical">
                <Setter Property="Width" Value="12"/>
                <Setter Property="Height" Value="Auto"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="ContextMenu">
        <Setter Property="Background" Value="{StaticResource WinPanelBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource WinTextBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource WinBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
    </Style>

    <Style TargetType="MenuItem">
        <Setter Property="Foreground" Value="{StaticResource WinTextBrush}"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Padding" Value="8,4"/>
        <Style.Triggers>
            <Trigger Property="IsHighlighted" Value="True">
                <Setter Property="Background" Value="#33AAB7C4"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="Separator">
        <Setter Property="Background" Value="{StaticResource WinSubtleBorderBrush}"/>
        <Setter Property="Margin" Value="4,2"/>
    </Style>

    <Style TargetType="GridSplitter">
        <Setter Property="Background" Value="{StaticResource WinSubtleBorderBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource WinSubtleBorderBrush}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="ShowsPreview" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="GridSplitter">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#FFD6DDE6"/>
            </Trigger>
        </Style.Triggers>
    </Style>
</ResourceDictionary>
