<Window x:Class="BlackbirdInterface.StartupWelcomeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title=""
        Height="200"
        Width="460"
        MinHeight="200"
        MinWidth="460"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        ResizeMode="NoResize"
        ShowInTaskbar="True"
        Background="{DynamicResource WinBgBrush}">
    <Grid Margin="14"
          MouseLeftButtonDown="Root_MouseLeftButtonDown">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="12"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0"
                               Text="Blackbird"
                               FontSize="20"
                               FontWeight="SemiBold"
                               VerticalAlignment="Center"/>
                    <Button Content="✕"
                            Grid.Column="1"
                            Width="22"
                            Height="20"
                            VerticalAlignment="Top"
                            Click="Close_Click"/>
                </Grid>

            <TextBlock Grid.Row="2"
                       Text="Start"
                       FontSize="16"
                       FontWeight="SemiBold"
                       TextWrapping="Wrap"/>

            <TextBlock Grid.Row="3"
                       Foreground="{DynamicResource WinMutedTextBrush}"
                       Text="Open a session archive or launch process targeting."/>

            <UniformGrid Grid.Row="5" Columns="3">
                <Button Margin="0,0,8,0"
                        Content="Launch"
                        Height="34"
                        Background="{DynamicResource WinAccentBrush}"
                        BorderBrush="{DynamicResource InputHoverBorderBrush}"
                        Foreground="{DynamicResource WinTextBrush}"
                        Click="Launch_Click"/>
                <Button Margin="0,0,8,0"
                        Content="Getting Started"
                        Height="34"
                        Click="GettingStarted_Click"/>
                <Button Content="Open Session"
                        Height="34"
                        Click="OpenTraceFile_Click"/>
            </UniformGrid>
    </Grid>
</Window>

